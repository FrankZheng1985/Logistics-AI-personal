# 物流获客AI - 自动部署工作流
# 当代码推送到 main 分支时自动部署到腾讯云服务器
# 优化：使用 Docker 缓存 + 并行构建，部署时间从 3-5 分钟降至 1-2 分钟

name: Deploy to Production

on:
  push:
    branches:
      - main
  workflow_dispatch:  # 允许手动触发

env:
  DOCKER_BUILDKIT: 1
  COMPOSE_DOCKER_CLI_BUILD: 1

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to Server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: 22
          command_timeout: 15m
          script: |
            set -e
            cd /home/ubuntu/logistics-ai
            
            # 启用 Docker BuildKit 加速构建
            export DOCKER_BUILDKIT=1
            export COMPOSE_DOCKER_CLI_BUILD=1
            
            # 拉取最新代码
            echo "📥 拉取最新代码..."
            git pull origin main
            
            # 检查哪些文件有变化，智能决定是否需要重建
            BACKEND_CHANGED=$(git diff HEAD~1 --name-only | grep -E '^backend/' | wc -l || echo 0)
            FRONTEND_CHANGED=$(git diff HEAD~1 --name-only | grep -E '^frontend/' | wc -l || echo 0)
            
            echo "📊 变更检测: Backend=$BACKEND_CHANGED, Frontend=$FRONTEND_CHANGED"
            
            # 执行数据库迁移（如果有新的迁移文件）
            echo "🗄️ 检查数据库迁移..."
            for migration in database/migrations/*.sql; do
              if [ -f "$migration" ]; then
                echo "执行迁移: $migration"
                docker exec -i logistics-ai-db psql -U admin -d logistics_ai < "$migration" 2>/dev/null || true
              fi
            done
            
            # 智能构建：只重建有变化的服务（使用缓存）
            echo "🔨 开始构建..."
            BUILD_SERVICES=""
            
            if [ "$BACKEND_CHANGED" -gt 0 ]; then
              BUILD_SERVICES="$BUILD_SERVICES backend"
            fi
            
            if [ "$FRONTEND_CHANGED" -gt 0 ]; then
              BUILD_SERVICES="$BUILD_SERVICES frontend"
            fi
            
            if [ -n "$BUILD_SERVICES" ]; then
              echo "构建服务: $BUILD_SERVICES"
              
              # 先停止需要重建的服务，避免容器名称冲突
              echo "🛑 停止需要重建的服务..."
              docker-compose -f docker-compose.prod.yml stop $BUILD_SERVICES 2>/dev/null || true
              docker-compose -f docker-compose.prod.yml rm -f $BUILD_SERVICES 2>/dev/null || true
              
              # 清理可能存在的孤立容器（名称冲突的根源）
              echo "🧹 清理孤立容器..."
              docker container prune -f 2>/dev/null || true
              
              # 使用缓存构建
              docker-compose -f docker-compose.prod.yml build $BUILD_SERVICES
            else
              echo "⏭️ 无需重建，只重启服务"
            fi
            
            # 启动/重启服务（使用 --force-recreate 确保容器被正确重建）
            echo "🚀 启动服务..."
            docker-compose -f docker-compose.prod.yml up -d --remove-orphans
            
            # 等待服务健康
            echo "⏳ 等待服务启动..."
            sleep 10
            
            # 健康检查
            echo "🏥 健康检查..."
            curl -sf http://localhost:8000/health > /dev/null && echo "✅ Backend OK" || echo "⚠️ Backend 启动中..."
            curl -sf http://localhost:3000 > /dev/null && echo "✅ Frontend OK" || echo "⚠️ Frontend 启动中..."
            
            # 清理旧镜像（保留最近的）
            docker image prune -f --filter "until=24h" 2>/dev/null || true
            
            echo "✅ 部署完成！"
